# class SameLine : public GUIElement
renders the next GUI Element on the same line as the previous GUI Element.<br>

## public SameLine(std::string name)
constructor for the SameLine class.<br>

### params:
* std::string name - the name of the SameLine.<br>

## public SameLine()
constructor for the SameLine class.<br>

### public void render() override
renders the next GUI Element on the same line as the previous GUI Element.<br>
